<div class="outer-container gallery-cont">
	<div class="container page-nav-cont">
		<h5 class="page-title">STREAM OF CONSCIOUSNESS</h5>
	</div>
	<div class="container">
		<!-- if galleries -->
		{{#if feeds}}
			
			<!--<h2>{{name}}
				<div class="pull-right text-muted">19th May 2014</div>
			</h2>-->
			<div class="row gallery-images">
					<!--{{#if heroImage}}
					<div class="col-sm-4 col-md-4 gallery-image">
						<img src="{{cloudinaryUrl heroImage width='680' height='680'}}" class="">
					</div>
					{{/if}}-->
					<div class="col-xs-12 feed-col">
					
							<ul class="grid effect-2" id="grid">
							{{#each feeds}}
								{{#each images}}	
									<li><a href="#"><img src="{{cloudinaryUrl crop='fit'}}"></a></li>
								{{/each}}	
							{{/each}}
							</ul>
					
					</div>
			</div>
			
		{{else}}
		<!-- else -->
		<h3 class="text-muted">There are no image galleries yet.</h3>
		{{/if}}
	</div>
</div>